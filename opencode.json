{
  "$schema": "https://opencode.ai/config.json",

  "mcp": {
    "servers": {
      "angular-cli": {
        "type": "local",
        "command": ["npx", "-y", "@angular/cli", "mcp"]
      }
    }
  }
}
