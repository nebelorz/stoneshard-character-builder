## 1. Styles Setup

- [x] 1.1 Add `progress-bar` and `progress-bar__fill` mixins to `_components.scss` and verify SCSS compiles without errors

## 2. Template Updates

- [x] 2.1 Import `NgIcon`, `provideIcons`, `phosphorBook`, and `phosphorPlusSquare` in `stat-controls.ts` and verify imports compile

- [x] 2.2 Replace the resource section in `stat-controls.html` with icon + label + value + progress bar structure for AP and SP

## 3. Component Styles

- [x] 3.1 Add resource-specific styles to `stat-controls.scss` using the new progress bar mixins and verify the component renders correctly

## 4. Verification

- [x] 4.1 Run `ng build` and verify no compilation errors

- [x] 4.2 Run `ng test` and verify all existing tests pass
